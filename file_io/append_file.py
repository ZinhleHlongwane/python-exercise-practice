def append_message(filename: str, message: str) -> None:
    """
    Appends a message to an existing file.

    Args:
        filename (str): file name
        message (str): text to append

    Returns:
        None
    """
