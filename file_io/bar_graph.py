def bar_graph(filename: str) -> str:
    """
    Creates a text-based bar graph of class averages.

    Each line in the file represents a class.
    Each number represents a student's mark.

    One '*' represents 10%.

    Args:
        filename (str): name of the file containing marks

    Returns:
        str: formatted bar graph
    """