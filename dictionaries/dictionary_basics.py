def get_student_mark(students: dict, name: str) -> int:
    """
    Returns a student's mark.

    Args:
        students (dict): student name to mark mapping
        name (str): student name

    Returns:
        int: student's mark
    """
