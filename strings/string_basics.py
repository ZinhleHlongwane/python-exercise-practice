def string_info(text: str) -> tuple[int, str]:
    """
    Returns the length of a string and its first character.

    Args:
        text (str): input string

    Returns:
        tuple[int, str]: length and first character
    """
