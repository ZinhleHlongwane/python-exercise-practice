def format_user(name: str, age: int) -> str:
    """
    Formats user information into a readable sentence.

    Args:
        name (str): user's name
        age (int): user's age

    Returns:
        str: formatted sentence
    """
