def play_game(secret: int, guess: int) -> str:
    """
    Compares a guess to the secret number.

    Args:
        secret (int): secret number
        guess (int): user's guess

    Returns:
        str: hint or success message
    """
