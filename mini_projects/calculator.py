def calculate(a: int, b: int, operator: str) -> int:
    """
    Performs a basic calculation.

    Args:
        a (int): first number
        b (int): second number
        operator (str): '+', '-', '*', '/'

    Returns:
        int: calculation result
    """
