def even_numbers(numbers: list[int]) -> list[int]:
    """
    Returns a list of even numbers using list comprehension.

    Args:
        numbers (list[int]): list of integers

    Returns:
        list[int]: even numbers only
    """
