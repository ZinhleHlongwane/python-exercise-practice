def clean_list(items: list[str]) -> list[str]:
    """
    Removes duplicates and sorts a list of strings.

    Args:
        items (list[str]): list with possible duplicates

    Returns:
        list[str]: cleaned list
    """
