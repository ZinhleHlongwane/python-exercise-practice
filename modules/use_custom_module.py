def use_addition(a: int, b: int) -> int:
    """
    Uses a function imported from a custom module.

    Args:
        a (int): first number
        b (int): second number

    Returns:
        int: result
    """
