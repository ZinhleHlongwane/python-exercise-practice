def unique_values(values: list[int]) -> set[int]:
    """
    Converts a list into a set of unique values.

    Args:
        values (list[int]): list with duplicates

    Returns:
        set[int]: unique values
    """
